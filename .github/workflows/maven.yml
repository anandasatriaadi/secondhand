# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven

on:
  push:
    branches: [ "production", "staging" ]
  pull_request:
    branches: [ "production", "staging" ]

env:
  JDBC_DATABASE_URL: jdbc:postgresql://ec2-54-157-16-196.compute-1.amazonaws.com:5432/d5so0n6mkg35tg?password=eb0ac26c540d10513347b6c506af9fb691c175e452f665ca07841097535e31ba&sslmode=require&user=ynwcsgxdejoesc
  JDBC_DATABASE_USERNAME: ynwcsgxdejoesc
  JDBC_DATABASE_PASSWORD: eb0ac26c540d10513347b6c506af9fb691c175e452f665ca07841097535e31ba

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 8
      uses: actions/setup-java@v3
      with:
        java-version: '8'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn clean install
